pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
	    environment {
			NUMBER_OF_ISSUES = "test_notification"
		    }
			    stage ('teste') {
				NUMBER_OF_ISSUES = "10"
				sh "echo '$NUMBER_OF_ISSUES'"
			    }
	    }
}
