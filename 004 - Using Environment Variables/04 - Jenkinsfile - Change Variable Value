pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
   	    stage ('teste') {
	    environment {
			NUMBER_OF_ISSUES = "test_notification"
		    }
		    steps {
			    NUMBER_OF_ISSUES = 10
				sh "echo '$NUMBER_OF_ISSUES'"
			    }
	    }
	}
}
