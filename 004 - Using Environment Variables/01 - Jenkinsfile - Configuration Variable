pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
		environment { 
			NUMBER_OF_ISSUES = "10" 
		}
            steps {
                echo "$NOTIFICATION_EMAIL" && echo "$NUMBER_OF_ISSUES"
            }
        }
    }
}
