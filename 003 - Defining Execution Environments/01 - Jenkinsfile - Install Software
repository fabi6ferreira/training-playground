pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	    	sh "apt-get install curl"
                sh "curl www.google.com"
            }
        }
    }
}
