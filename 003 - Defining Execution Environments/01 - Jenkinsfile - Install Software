pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	    	sh "apt install curl"
                sh "curl www.google.com"
            }
        }
    }
}
